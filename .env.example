# Sanity API tokens for content sync
# Generate these at https://sanity.io/manage

# Read token (required for scanning and reading documents)
SANITY_SYNC_READ_TOKEN=your-read-token-here

# Write token (required for executing sync operations)
SANITY_SYNC_WRITE_TOKEN=your-write-token-here
